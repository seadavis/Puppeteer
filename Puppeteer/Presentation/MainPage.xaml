<Page x:Class="Puppeteer.Presentation.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:Puppeteer.Presentation"
      xmlns:uen="using:Uno.Extensions.Navigation.UI"
      xmlns:utu="using:Uno.Toolkit.UI"
      xmlns:um="using:Uno.Material"
      NavigationCacheMode="Required"
      Loaded="Page_Loaded"
      Background="{ThemeResource BackgroundBrush}">
  <ScrollViewer IsTabStop="True">
    <Grid utu:SafeArea.Insets="VisibleBounds">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="2*" />
        <RowDefinition Height="*" />
      </Grid.RowDefinitions>
      <utu:NavigationBar Content="{Binding Title}" />

            <WebView2 Grid.Row="1" x:Name="SceneView" Height="800" Width="600" />
            <Border Grid.Row="2" x:Name="ImageBorder" Height="400" Width="400"></Border>
        </Grid>
  </ScrollViewer>
</Page>
